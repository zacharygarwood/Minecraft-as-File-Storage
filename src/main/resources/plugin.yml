name: 'MAFS'
version: '1.0'
author: 'Zach'
main: com.mafs.Main
api-version: '1.20'
commands:
  encode:
    description: 'Encodes a given file in Minecraft blocks and a structure representing the file is generated in front of the player. NOTE: The file MUST be in the "files" directory where your server is running'
    usage: /<command> <filename>
  decode:
    description: 'Decodes a previously encoded file from the structure in front of the player. Stand where you ran /encode. The file is written to the "files" directory where your server is running and existing files are not overwritten'
    usage: /<command> <filename>
